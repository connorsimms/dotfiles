- defaults:
    link:
      create: true
      relink: true

- clean: ['~', '~/.config']

- link:
    ~/.config/nvim: nvim
    ~/.vim: vim
    ~/.vimrc: vimrc
    ~/.config/tmux: tmux

- shell:
  - [git submodule update --init --recursive, Installing submodules]
