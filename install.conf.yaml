- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.config/nvim: nvim
    ~/.vim: vim
    ~/.vimrc: vimrc
    ~/.config/tmux: tmux

- shell:
  - [git submodule update --init --recursive, Installing submodules]
